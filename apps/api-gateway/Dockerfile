# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @multivendor-eshop/api-gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t multivendor-eshop-api-gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system multivendor-eshop-api-gateway && \
          adduser --system -G multivendor-eshop-api-gateway multivendor-eshop-api-gateway

COPY apps/api-gateway/dist multivendor-eshop-api-gateway/
COPY apps/api-gateway/package.json multivendor-eshop-api-gateway/
RUN chown -R multivendor-eshop-api-gateway:multivendor-eshop-api-gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix multivendor-eshop-api-gateway --omit=dev -f install

CMD [ "node", "multivendor-eshop-api-gateway" ]
